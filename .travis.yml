language: swift
osx_image: xcode9.2
xcode_workspace: PlayerControls

before_install:
- brew update
- brew install sourcery
- bundle install
- cd SnapshotTests
- carthage update --platform iOS

script:
- cd ..
- fastlane test
